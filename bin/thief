#!/usr/bin/env ruby
require File.expand_path('../../lib/boot', __FILE__)

require 'thief'

db_config = File.expand_path('../../config/database.yml', __FILE__)
Thief.setup(db_config)

# # # # # # # # # # # # # # # # # # # # #

Thief::Person.delete_all

Thief::IMDb::ETL.fetch
Thief::IMDb::Integrator.integrate

# Thief.fetch
# Thief.integrate

Thief::Person.all.each { |person| puts person.name }

#require 'rest_client'

#response = RestClient.get 'http://v1.d-api.de/parlament.bund.politiker/get', :bundestag_id => 'ackermann_jens' #, :accept => :json

#puts response.body

# game = Mastermind::Game.new(STDOUT)
# game.start(%w[r g y c])
# while guess = gets
#   game.guess guess.split
# end
